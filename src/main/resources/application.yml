#RabbitMQ Configuration
spring:
  cloud:
    stream:
      bindings:
        eventConsumer-in-0:
          destination: sweng.events
          binder: cloud_rabbit
      binders:
        cloud_rabbit:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: cow.rmq2.cloudamqp.com
                port: 5672
                username: admin
                password: password
                virtual-host: ghkcurap
